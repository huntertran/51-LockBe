@model IEnumerable<QuoteApi.Models.SingleQuote>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Quote)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Self)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Quote)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Self)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SingleQuoteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SingleQuoteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SingleQuoteId })
        </td>
    </tr>
}

</table>
